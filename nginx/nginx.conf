worker_processes auto;

events {
    worker_connections 1024;
}

http {
    server {
        listen 80;
        server_name _;

        location /health {
            access_log off;
            default_type text/plain;
            return 200 'OK';
        }
    }
}
