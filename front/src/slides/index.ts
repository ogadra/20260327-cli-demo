import type { ComponentType } from "react";
import Slide0 from "./Slide0";

/** Ordered array of slide components indexed by page number. */
const slides: ComponentType[] = [Slide0];

export default slides;
